# ============================================
# DISABLED: Twilio SMS Extension - January 25, 2026
#
# This extension is disabled. FCM push notifications provide equivalent functionality.
# See docs/removetwilio.md for reactivation instructions.
#
# IMPORTANT: Credentials have been removed from this file for security.
# If reactivating, use Firebase secrets or environment variables:
#   firebase functions:secrets:set TWILIO_ACCOUNT_SID
#   firebase functions:secrets:set TWILIO_AUTH_TOKEN
#   firebase functions:secrets:set TWILIO_PHONE_NUMBER
#
# Or add to extensions/.env (gitignored):
#   TWILIO_ACCOUNT_SID=your_account_sid
#   TWILIO_AUTH_TOKEN=your_auth_token
#   TWILIO_PHONE_NUMBER=your_phone_number
# ============================================

name: twilio-send-message
ref: twilio/send-message@0.3.4

params:
  # SECURITY: Use environment variables - never commit credentials
  # Add these to extensions/.env (gitignored) or Firebase secrets
  - param: TWILIO_ACCOUNT_SID
    value: ${TWILIO_ACCOUNT_SID}

  - param: TWILIO_AUTH_TOKEN
    value: ${TWILIO_AUTH_TOKEN}

  - param: TWILIO_PHONE_NUMBER
    value: ${TWILIO_PHONE_NUMBER}

  - param: FIRESTORE_COLLECTION_PATH
    value: sms_queue

  - param: LOCATION
    value: us-central1
